

const initialState = {
    isLoading: false
}

const motorsReducer = (state = initialState, action) => {
    switch (action.type) {


        default:
            return state
    }
}





export default motorsReducer;